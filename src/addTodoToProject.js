// This will add any Todo to a Project

// how do i add a push function to this

function addTodoToProject(title,description,dueDate,priority) {
    const newTodo = new addTodoToProject(title,description,dueDate,priority);

}

export {addTodoToProject}